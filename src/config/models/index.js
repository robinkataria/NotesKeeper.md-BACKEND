const User = require('./users.model')
const Notebook = require('./notebooks.model')
const Note = require('./notes.model')
const Todo = require('./todos.model')

module.exports = {
    User,
    Notebook,
    Note,
    Todo
}